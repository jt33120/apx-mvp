"""The bounded runtime probes (Story 4.12) — properties proven by EXERCISING the product, not by
reading it. A probe walks an enumerated registry, executes every action on it against a real store,
and asserts a whole-system invariant after each one."""
